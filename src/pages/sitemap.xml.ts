import type { APIRoute } from 'astro';
import { dayTours, destinations, tours } from '@/lib/content';
export const GET: APIRoute = ({ site, url }) => { const base = site || url; const paths = ['/', '/tours/', '/day-tours/', '/destinations/', '/about/', '/plan-my-tour/', '/contact/', ...tours.map((item) => `/tours/${item.slug}/`), ...dayTours.map((item) => `/day-tours/${item.slug}/`), ...destinations.map((item) => `/destinations/${item.slug}/`)]; const xml = `<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map((path) => `<url><loc>${new URL(path, base).toString()}</loc></url>`).join('')}</urlset>`; return new Response(xml, { headers: { 'Content-Type': 'application/xml' } }); };
